#!/bin/bash
file=$1

dir=data/stepanek

xsh <<< "open $dir/$file.treex.gz ; rm //pml:zone[0=count(pml:sentence)]/../..  ; save :b"
treex github/tecto2umr/scen/t2umr.scen -- \
      "$dir"/"$file".treex.gz \
      > "$dir"/"$file".umr \
      2> "$dir"/"$file".log
